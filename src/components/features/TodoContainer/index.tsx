import styles from './styles.module.css';

const TodoContainer = () => {
  return (
    <section className={styles.container}>
      <h3>ToDoList</h3>
    </section>
  );
};

export default TodoContainer;
